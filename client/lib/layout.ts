/** Main app column: mobile-first, wider from md up. */
export const pageMaxWidthClass = "mx-auto w-full max-w-md md:max-w-2xl lg:max-w-3xl";

/** Auth forms: slightly narrower on small screens, same breakpoints on md+. */
export const authPageMaxWidthClass = "mx-auto w-full max-w-sm md:max-w-sm lg:max-w-sm";
